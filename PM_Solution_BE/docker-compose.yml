version: "3.8"
services:
  db:
    image: postgres:16
    container_name: issueviz-db
    environment:
      POSTGRES_DB: issueviz
      POSTGRES_USER: issueviz
      POSTGRES_PASSWORD: issueviz
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
